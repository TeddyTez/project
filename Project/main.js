const change = document.getElementById("content");

const changeContent = () => {
  let newContent =
    "It is a distributed version control system, which means that your local copy of the code is a complete repository.";

  change.textContent = newContent;
};
